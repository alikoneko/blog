<h1> Welcome, Ali </h1>
<div>
    <table>
    <% @posts.each do |post|%>
        <tr>
            <td><%= post.title %></td>
            <td><%= link_to "edit", [:edit, :admin, post] %></td>
            <td><%= post.status %></td>
        </tr>
    <% end %>
    </table>
</div>